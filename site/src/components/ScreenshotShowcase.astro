<section class="showcase" id="screenshots" aria-labelledby="showcase-heading">
  <div class="showcase__intro">
    <p class="eyebrow">Designed for macOS</p>
    <h2 id="showcase-heading">Quick when you need it. Quiet when you don’t.</h2>
  </div>

  <div class="showcase__grid">
    <figure class="showcase-card showcase-card--search">
      <div class="placeholder placeholder--wide" role="img" aria-label="Placeholder for a screenshot of search in Clipy Secure">
        <div class="mock-window" aria-hidden="true">
          <div class="mock-window__bar">
            <div class="traffic"><i></i><i></i><i></i></div>
            <span>Clipboard History</span>
          </div>
          <div class="mock-window__search"><span>⌕</span><span>meeting notes</span></div>
          <div class="mock-window__results">
            <span></span><span></span><span></span>
          </div>
        </div>
        <span class="placeholder__label">Search workflow · 1600 × 1000 px</span>
      </div>
      <figcaption>
        <strong>Find anything fast.</strong>
        <span>Search your history without writing a plaintext index to disk.</span>
      </figcaption>
    </figure>

    <figure class="showcase-card showcase-card--controls">
      <div class="placeholder placeholder--square" role="img" aria-label="Placeholder for a screenshot of Clipy Secure privacy preferences">
        <div class="mock-window mock-window--settings" aria-hidden="true">
          <div class="mock-window__bar">
            <div class="traffic"><i></i><i></i><i></i></div>
            <span>Privacy</span>
          </div>
          <div class="mock-settings">
            <div><span class="toggle toggle--on"></span><span></span></div>
            <div><span class="toggle toggle--on"></span><span></span></div>
            <div><span class="toggle"></span><span></span></div>
          </div>
        </div>
        <span class="placeholder__label">Privacy controls · 1440 × 1080 px</span>
      </div>
      <figcaption>
        <strong>Choose what gets remembered.</strong>
        <span>Exclude apps and automatically ignore concealed content.</span>
      </figcaption>
    </figure>
  </div>
</section>

<style>
  .showcase {
    width: min(100% - 2rem, 74rem);
    margin-inline: auto;
    padding-block: clamp(5rem, 10vw, 8rem);
  }

  .showcase__intro {
    display: grid;
    grid-template-columns: minmax(9rem, 0.35fr) minmax(0, 1fr);
    gap: 2rem;
    align-items: start;
    margin-bottom: clamp(2.5rem, 6vw, 4rem);
  }

  .eyebrow {
    margin: 0.25rem 0 0;
    color: var(--color-accent, #6ee7c5);
    font-size: 0.76rem;
    font-weight: 720;
    letter-spacing: 0.11em;
    text-transform: uppercase;
  }

  h2 {
    max-width: 17ch;
    margin: 0;
    color: var(--color-text, #f5f7f6);
    font-size: clamp(2rem, 4vw, 3.6rem);
    line-height: 1.05;
    letter-spacing: -0.04em;
  }

  .showcase__grid {
    display: grid;
    grid-template-columns: minmax(0, 1.25fr) minmax(18rem, 0.75fr);
    gap: 1rem;
    align-items: start;
  }

  figure {
    min-width: 0;
    margin: 0;
  }

  .placeholder {
    position: relative;
    display: grid;
    place-items: center;
    overflow: hidden;
    border: 1px solid var(--color-border, #2c3935);
    border-radius: clamp(1.15rem, 2.8vw, 1.8rem);
    background:
      radial-gradient(circle at 80% 10%, rgba(110, 231, 197, 0.14), transparent 40%),
      linear-gradient(145deg, #101817, #0a0e0d);
  }

  .placeholder::before {
    position: absolute;
    inset: 0;
    background: linear-gradient(120deg, transparent 20%, rgba(255, 255, 255, 0.025), transparent 70%);
    content: "";
  }

  .placeholder--wide {
    aspect-ratio: 8 / 5;
  }

  .placeholder--square {
    aspect-ratio: 4 / 3;
  }

  .mock-window {
    position: relative;
    width: 78%;
    overflow: hidden;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 0.75rem;
    background: rgba(32, 36, 35, 0.96);
    box-shadow: 0 1.5rem 3rem rgba(0, 0, 0, 0.45);
  }

  .mock-window--settings {
    width: 76%;
  }

  .mock-window__bar {
    position: relative;
    display: flex;
    height: 2rem;
    align-items: center;
    justify-content: center;
    border-bottom: 1px solid rgba(255, 255, 255, 0.08);
    color: #b6bfbc;
    font-size: clamp(0.52rem, 1vw, 0.7rem);
  }

  .traffic {
    position: absolute;
    left: 0.7rem;
    display: flex;
    gap: 0.3rem;
  }

  .traffic i {
    display: block;
    width: 0.48rem;
    height: 0.48rem;
    border-radius: 50%;
    background: #59625f;
  }

  .mock-window__search {
    display: flex;
    align-items: center;
    gap: 0.55rem;
    margin: 0.85rem;
    border: 1px solid rgba(255, 255, 255, 0.12);
    border-radius: 0.42rem;
    padding: 0.5rem 0.65rem;
    background: rgba(0, 0, 0, 0.18);
    color: #aeb8b4;
    font-size: clamp(0.5rem, 1vw, 0.68rem);
  }

  .mock-window__results {
    display: grid;
    gap: 0.5rem;
    padding: 0 0.85rem 1rem;
  }

  .mock-window__results span {
    height: clamp(1.1rem, 2.3vw, 1.8rem);
    border-radius: 0.35rem;
    background: rgba(255, 255, 255, 0.055);
  }

  .mock-window__results span:first-child {
    background: linear-gradient(90deg, rgba(20, 150, 126, 0.9), rgba(16, 124, 110, 0.85));
  }

  .mock-settings {
    display: grid;
    gap: 0.85rem;
    padding: 1.2rem;
  }

  .mock-settings > div {
    display: grid;
    grid-template-columns: 1.65rem 1fr;
    gap: 0.75rem;
    align-items: center;
  }

  .mock-settings > div > span:last-child {
    display: block;
    width: 78%;
    height: 0.4rem;
    border-radius: 999px;
    background: rgba(255, 255, 255, 0.14);
  }

  .toggle {
    position: relative;
    display: block;
    width: 1.65rem;
    height: 0.92rem;
    border-radius: 999px;
    background: #4a514f;
  }

  .toggle::after {
    position: absolute;
    top: 0.14rem;
    left: 0.15rem;
    width: 0.64rem;
    height: 0.64rem;
    border-radius: 50%;
    background: #d9dddb;
    content: "";
  }

  .toggle--on {
    background: #178f80;
  }

  .toggle--on::after {
    left: 0.86rem;
  }

  .placeholder__label {
    position: absolute;
    right: 1rem;
    bottom: 0.8rem;
    color: rgba(230, 238, 235, 0.5);
    font-size: clamp(0.55rem, 0.9vw, 0.7rem);
    letter-spacing: 0.03em;
  }

  figcaption {
    padding: 1.1rem 0.2rem 0;
  }

  figcaption strong,
  figcaption span {
    display: block;
  }

  figcaption strong {
    color: var(--color-text, #f5f7f6);
    font-size: 1rem;
  }

  figcaption span {
    margin-top: 0.32rem;
    color: var(--color-text-muted, #b5c0bd);
    font-size: 0.86rem;
    line-height: 1.5;
  }

  @media (max-width: 760px) {
    .showcase__intro,
    .showcase__grid {
      grid-template-columns: 1fr;
    }

    .showcase__intro {
      gap: 1rem;
    }

    .showcase__grid {
      gap: 2.5rem;
    }

    .placeholder--square {
      aspect-ratio: 8 / 5;
    }

    .mock-window--settings {
      width: 62%;
    }
  }

  @media (max-width: 500px) {
    .showcase {
      width: min(100% - 1.25rem, 74rem);
    }
  }
</style>
